interface:
  display_name: "ReviewMark"
  short_description: "Review Markdown with comments inside the file"
  default_prompt: "Use $reviewmark to review this Markdown file with ReviewMark comments inside the file."
